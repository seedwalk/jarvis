# Jarvis Runtime

Directorio local de runtime para comandos `/jarvis-*`.

## Archivos

- `config.json`: configuracion local (creado por `/jarvis-setup`).
- `milestones/`: notas y estado por milestone.

## Nota

No guardar secretos en `config.json`.
Usar variables de entorno para tokens/API keys.
